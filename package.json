{
  "name": "luxeglow-store",
  "version": "1.0.0",
  "description": "E-commerce website template",
  "scripts": {
    "clean": "node -e \"const fs = require('fs'); try { fs.rmSync('dist', { recursive: true, force: true }); } catch(e) {}\"",
    "release": "npm run clean && node tools/copy-release.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "license": "MIT"
}

